// @flow

import variable from "~/constants/themeVars";

export default (variables /*: * */ = variable) => {
  const linkTheme = {
    "NativeBase.Text": {
      color: "#1677d2",
      fontSize: 14
    },
    display: "flex"
  };

  return linkTheme;
};
