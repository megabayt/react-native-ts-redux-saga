// @flow

import variable, { fontMaker } from "~/constants/themeVars";
import { scaleH, scaleW } from "~/utils/helpers";
import { StyleSheet } from "react-native";

export default (variables /*: * */ = variable) => {
  const graphTheme = {
    marginTop: 10,
    position: "relative"
  };

  return graphTheme;
};
